version: '3.8'
services:
  db:
    image: postgres:16-alpine
    container_name: panconnect_postgres_stage
    restart: always
    environment:
      POSTGRES_USER: meghraj
      POSTGRES_PASSWORD: Monya848
      POSTGRES_DB: panconnect_stage
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: